language: generic
services:
  - docker

cache:
  directories:
    - $HOME/.ccache

git:
  quiet: true

env:
  global:
    - BEFORE_INSTALL_TARGET_DEPENDENCIES='./install_opengr.sh'
    - CATKIN_LINT=true
    - CCACHE_DIR=$HOME/.ccache
    - ROS_REPO=ros
    - UPSTREAM_WORKSPACE=file
    - BUILDER="colcon"
    - CODE_COVERAGE="codecov.io"

  matrix:
    - ROS_DISTRO="melodic" ROS_REPO="main"
    - ROS_DISTRO="melodic" ROS_REPO="testing"

install:
  - git clone --quiet --depth=1 -b pr-coverage https://github.com/Briancbn/industrial_ci.git .ci_config

script:
  - .ci_config/travis.sh
